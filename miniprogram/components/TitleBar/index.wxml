<view style="width: 100%; background: white; height: {{CustomBar - StatusBar}}px" />
<view class="top" style="height: {{StatusBar}}px;">
  <text class="back cuIcon-back" bind:tap="back"></text>
  <text class="title">{{title}}</text>
</view>
