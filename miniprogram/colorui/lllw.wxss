.ll-btn {
  color: white;
  font-weight: 600;
  border-radius: 16rpx;
  background-color: var(--brand-green);
  display: flex;
  align-items: center;
  justify-content: center;
}

.ll-btn.ll-btn-yellow {
  background-color: var(--brand-yellow);
}